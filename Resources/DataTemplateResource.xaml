<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:model="clr-namespace:IT008_QuanLyBanHang.Model"
                    xmlns:view="clr-namespace:IT008_QuanLyBanHang.View">
    <DataTemplate DataType="{x:Type model:BatchProduct}">
        <view:Card_ThongTinSanPham ProductName="{Binding Product.ProductName}"
                               ManufactureDate="{Binding Batch.ManufactureDate}"
                               ExpirationDate="{Binding Batch.ExpirationDate}"
                               Price="{Binding Batch.Price}"
                               Quantity="{Binding Batch.Quantity}"
                               Visibility="{Binding Visibility}"/>
    </DataTemplate>
</ResourceDictionary>